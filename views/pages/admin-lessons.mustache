<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="/css/global.min.css" rel="stylesheet">
    <link href="/css/admin.css?v={{timestamp}}" rel="stylesheet">
    <style>
        /* CORREÇÃO CRÍTICA - FORÇAR LAYOUT CORRETO */
        .admin-lessons-page .container-fluid {
            padding: 0 !important;
            margin: 0 !important;
            max-width: none !important;
        }
        
        .admin-lessons-page .row {
            margin: 0 !important;
        }
        
        .admin-lessons-page .col-md-9,
        .admin-lessons-page .col-lg-10,
        .admin-lessons-page .main-content {
            flex: none !important;
            width: calc(100vw - 280px) !important;
            max-width: none !important;
            margin-left: 280px !important;
            margin-right: 0 !important;
            padding-left: 2rem !important;
            padding-right: 2rem !important;
            position: relative !important;
        }
        
        /* Estilos específicos para maior visibilidade */
        .main-content {
            padding: 2rem !important;
            background: #f8fafc;
        }
        
        .admin-header {
            background: linear-gradient(135deg, #7c3aed 0%, #6d28d9 100%);
            color: white;
            padding: 3rem;
            border-radius: 1rem;
            margin-bottom: 2rem;
            box-shadow: 0 10px 25px rgba(124, 58, 237, 0.3);
        }
        
        .admin-title {
            font-size: 3rem;
            font-weight: 800;
            margin-bottom: 1rem;
        }
        
        .admin-subtitle {
            font-size: 1.3rem;
            opacity: 0.9;
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #7c3aed 0%, #6d28d9 100%);
            border: none;
            padding: 1rem 2rem;
            font-size: 1.1rem;
            font-weight: 600;
            border-radius: 0.75rem;
            box-shadow: 0 4px 15px rgba(124, 58, 237, 0.4);
            transition: all 0.3s ease;
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(124, 58, 237, 0.5);
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
            margin-bottom: 2rem;
        }
        
        .stat-card {
            background: white;
            padding: 2.5rem;
            border-radius: 1rem;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            border-left: 6px solid #7c3aed;
            transition: all 0.3s ease;
        }
        
        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 30px rgba(0,0,0,0.15);
        }
        
        .stat-number {
            font-size: 3.5rem;
            font-weight: 800;
            color: #1e293b;
            margin-bottom: 0.5rem;
        }
        
        .stat-label {
            font-size: 1.2rem;
            color: #64748b;
            font-weight: 500;
        }
        
        .filters-section {
            background: white;
            padding: 2rem;
            border-radius: 1rem;
            margin-bottom: 2rem;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }
        
        .search-container {
            position: relative;
        }
        
        .search-input {
            padding: 1rem 1rem 1rem 3.5rem;
            font-size: 1.1rem;
            border: 3px solid #e5e7eb;
            border-radius: 0.75rem;
            transition: all 0.3s ease;
        }
        
        .search-input:focus {
            border-color: #7c3aed;
            box-shadow: 0 0 0 4px rgba(124, 58, 237, 0.1);
        }
        
        .search-icon {
            position: absolute;
            left: 1.2rem;
            top: 50%;
            transform: translateY(-50%);
            font-size: 1.2rem;
            color: #64748b;
        }
        
        .form-select {
            padding: 1rem;
            font-size: 1.1rem;
            border: 3px solid #e5e7eb;
            border-radius: 0.75rem;
        }
        
        .lessons-table {
            background: white;
            border-radius: 1rem;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }
        
        .table {
            margin: 0;
            font-size: 1rem;
        }
        
        .table thead {
            background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
        }
        
        .table thead th {
            padding: 2rem 1.5rem;
            font-weight: 700;
            font-size: 1rem;
            color: #1e293b;
            border: none;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .table tbody td {
            padding: 2rem 1.5rem;
            border-bottom: 2px solid #f1f5f9;
            vertical-align: middle;
        }
        
        .table tbody tr:hover {
            background: #f8fafc;
        }
        
        .lesson-name {
            font-size: 1.3rem;
            font-weight: 700;
            color: #1e293b;
            margin-bottom: 0.5rem;
        }
        
        .lesson-description {
            font-size: 1rem;
            color: #64748b;
            line-height: 1.5;
        }
        
        .package-info {
            display: flex;
            align-items: center;
            gap: 1rem;
            font-size: 1.1rem;
            font-weight: 600;
        }
        
        .package-info i {
            font-size: 1.5rem;
            color: #7c3aed;
        }
        
        .badge {
            font-size: 1rem;
            padding: 0.75rem 1.5rem;
            border-radius: 0.5rem;
            font-weight: 600;
        }
        
        .action-buttons {
            display: flex;
            gap: 0.5rem;
            justify-content: center;
        }
        
        .btn-sm {
            padding: 0.4rem 0.8rem;
            font-size: 1.2rem;
            border-radius: 0.4rem;
            border: 1px solid;
            transition: all 0.2s ease;
            min-width: 35px;
            height: 35px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .btn-outline-primary {
            border-color: #7c3aed;
            color: #7c3aed;
            background: rgba(124, 58, 237, 0.05);
        }
        
        .btn-outline-primary:hover {
            background: #7c3aed;
            color: white;
            border-color: #7c3aed;
        }
        
        .btn-outline-danger {
            border-color: #dc2626;
            color: #dc2626;
            background: rgba(220, 38, 38, 0.05);
        }
        
        .btn-outline-danger:hover {
            background: #dc2626;
            color: white;
            border-color: #dc2626;
        }
        
        .progress {
            height: 12px;
            border-radius: 6px;
            background-color: #e5e7eb;
        }
        
        .progress-bar {
            border-radius: 6px;
            background: linear-gradient(90deg, #059669 0%, #047857 100%);
        }
        
        @media (max-width: 768px) {
            /* Correção para mobile - sidebar oculta */
            .admin-lessons-page .col-md-9,
            .admin-lessons-page .col-lg-10,
            .admin-lessons-page .main-content {
                margin-left: 0 !important;
                width: 100vw !important;
                padding-left: 1rem !important;
                padding-right: 1rem !important;
            }
            
            .admin-title {
                font-size: 2rem;
            }
            
            .stats-grid {
                grid-template-columns: 1fr;
            }
            
            .action-buttons {
                flex-direction: column;
                gap: 0.5rem;
            }
        }
    </style>
</head>
<body class="admin-lessons-page">
    {{> header}}

    <div class="container-fluid">
        <div class="row">
            {{> sidebar}}
            
            <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 main-content">
                <!-- Cabeçalho da Página -->
                <div class="admin-header">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h1 class="admin-title">
                                <i class="fas fa-play-circle me-3"></i>
                                Administração de Aulas
                            </h1>
                            <p class="admin-subtitle mb-0">Gerencie todas as aulas da plataforma com facilidade</p>
                        </div>
                        <button class="btn btn-primary btn-lg" id="addLessonBtn">
                            <i class="fas fa-plus me-2"></i>Nova Aula
                        </button>
                    </div>
                </div>

                <!-- Estatísticas -->
                <div class="stats-grid">
                    <div class="stat-card">
                        <div class="stat-number">{{stats.total_lessons}}</div>
                        <div class="stat-label">
                            <i class="fas fa-play-circle me-2"></i>Total de Aulas
                        </div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-number">{{stats.packages_with_lessons}}</div>
                        <div class="stat-label">
                            <i class="fas fa-box me-2"></i>Pacotes com Aulas
                        </div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-number">{{stats.avg_lesson_number}}</div>
                        <div class="stat-label">
                            <i class="fas fa-chart-line me-2"></i>Média por Pacote
                        </div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-number">{{stats.max_lesson_number}}</div>
                        <div class="stat-label">
                            <i class="fas fa-crown me-2"></i>Maior Sequência
                        </div>
                    </div>
                </div>

                <!-- Filtros -->
                <div class="filters-section">
                    <div class="row g-4">
                        <div class="col-md-4">
                            <div class="search-container">
                                <i class="fas fa-search search-icon"></i>
                                <input type="text" class="form-control search-input" id="searchLessons" 
                                       placeholder="Buscar aulas por nome...">
                            </div>
                        </div>
                        <div class="col-md-3">
                            <select class="form-select" id="filterPackage">
                                <option value="">📦 Todos os Pacotes</option>
                                {{#packages}}
                                <option value="{{id}}">{{name}}</option>
                                {{/packages}}
                            </select>
                        </div>
                        <div class="col-md-3">
                            <select class="form-select" id="sortLessons">
                                <option value="package_order">📊 Ordenar por Pacote</option>
                                <option value="name">🔤 Ordenar por Nome</option>
                                <option value="completion_rate">📈 Taxa de Conclusão</option>
                                <option value="created_at">📅 Data de Criação</option>
                            </select>
                        </div>
                        <div class="col-md-2">
                            <button class="btn btn-outline-secondary w-100" id="resetFilters">
                                <i class="fas fa-undo me-1"></i>Limpar
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Tabela de Aulas -->
                <div class="lessons-table">
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th width="5%">
                                        <i class="fas fa-grip-vertical text-muted"></i>
                                    </th>
                                    <th width="40%">📚 Aula</th>
                                    <th width="20%">📦 Pacote</th>
                                    <th width="10%">🔢 Sequência</th>
                                    <th width="15%">📈 Progresso</th>
                                    <th width="10%">⚡ Ações</th>
                                </tr>
                            </thead>
                            <tbody id="lessonsTableBody">
                                {{#lessons}}
                                <tr data-lesson-id="{{id}}" data-package-id="{{package_id}}" class="lesson-row">
                                    <td class="drag-handle">
                                        <i class="fas fa-grip-vertical text-muted"></i>
                                    </td>
                                    <td>
                                        <div class="lesson-info">
                                            <div class="lesson-name">{{name}}</div>
                                            <div class="lesson-description">
                                                {{#description}}{{description}}{{/description}}
                                                {{^description}}Sem descrição disponível{{/description}}
                                            </div>
                                        </div>
                                    </td>
                                    <td>
                                        <div class="package-info">
                                            <i class="{{package_icon}} text-primary"></i>
                                            <span>{{package_name}}</span>
                                        </div>
                                    </td>
                                    <td>
                                        <span class="badge bg-secondary">
                                            {{lesson_number}}/{{order_sequence}}
                                        </span>
                                    </td>
                                    <td>
                                        <div class="completion-stats">
                                            <div class="progress mb-2">
                                                <div class="progress-bar" 
                                                     style="width: {{completion_rate}}%"></div>
                                            </div>
                                            <small class="text-muted">
                                                {{completion_rate}}% ({{completion_count}} usuários)
                                            </small>
                                        </div>
                                    </td>
                                    <td>
                                        <div class="action-buttons">
                                            <button class="btn btn-sm btn-outline-primary edit-lesson-btn" 
                                                    data-lesson-id="{{id}}" data-lesson-name="{{name}}" title="Editar Nome">
                                                ✏️
                                            </button>
                                            <button class="btn btn-sm btn-outline-danger delete-lesson-btn" 
                                                    data-lesson-id="{{id}}" data-lesson-name="{{name}}" title="Excluir">
                                                🗑️
                                            </button>
                                        </div>
                                    </td>
                                </tr>
                                {{/lessons}}
                            </tbody>
                        </table>
                    </div>

                    <!-- Estado Vazio -->
                    <div id="emptyState" class="empty-state text-center py-5" style="display: none;">
                        <div class="empty-state-content">
                            <i class="fas fa-play-circle text-muted mb-3" style="font-size: 4rem;"></i>
                            <h3 class="text-muted">Nenhuma aula encontrada</h3>
                            <p class="text-muted mb-4">Não há aulas que correspondam aos filtros aplicados.</p>
                            <button class="btn btn-primary" id="clearFiltersBtn">
                                <i class="fas fa-filter me-2"></i>Limpar Filtros
                            </button>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>



    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/admin-lessons.js?v={{timestamp}}"></script>
</body>
</html> 