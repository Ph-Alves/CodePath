{{!-- Empty State para Aulas - CodePath --}}
<div class="empty-state empty-medium empty-warning" 
     data-aos="fade-up" 
     data-illustration="book">
    <div class="empty-content">
        <!-- Ilustração SVG -->
        <div class="empty-illustration">
            <!-- Conteúdo será inserido dinamicamente -->
        </div>
        
        <!-- Texto do Estado Vazio -->
        <div class="empty-text">
            <h3 class="empty-title">Nenhuma aula disponível</h3>
            <p class="empty-message">Este pacote ainda não possui aulas publicadas. Em breve teremos conteúdo incrível para você aprender!</p>
        </div>
        
        <!-- Botões de Ação -->
        <div class="empty-actions">
            <a href="/careers" class="btn btn-primary empty-action-btn">
                <span class="btn-text">Explorar Outros Pacotes</span>
                <div class="btn-ripple"></div>
            </a>
        </div>
        
        <!-- Elementos Decorativos -->
        <div class="empty-decorations">
            <div class="decoration decoration-1"></div>
            <div class="decoration decoration-2"></div>
            <div class="decoration decoration-3"></div>
        </div>
    </div>
</div> 